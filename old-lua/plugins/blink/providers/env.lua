return {
  name = 'Env',
  module = 'blink-cmp-env',
  opts = {
    --- FIXME: item_kind = require('blink.cmp').CompletionItemKind.Variable,
    show_binaries = false,
    show_documentation_window = true,
  },
}
